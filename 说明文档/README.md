# 说明文档目录

这里保存每个实验的个人学习材料，不直接作为最终课程报告。

目录约定：

```text
说明文档/
├─ 实验一-服务单元测试/
│  └─ main.tex
├─ 实验二-持续集成测试/
│  └─ main.tex
└─ 实验三-程序性能跟踪/
   └─ main.tex
```

所有文档使用 UTF-8 中文编写。用 TeXworks 打开相应的 `main.tex`，排版工具选择 XeLaTeX 后编译。LaTeX 辅助文件和 PDF 已通过 `.gitignore` 排除。
